<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Marketplace Industrial</title>
    
    <!-- Importando os estilos necessários -->
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body class="auth-page">

    <div class="auth-container">
        <h1 class="auth-logo">Marketplace Industrial</h1>

        <div class="auth-box">
            <h2 class="auth-box__title">Crie uma Nova Senha</h2>
            <p class="auth-box__subtitle">Sua nova senha deve ter no mínimo 6 caracteres.</p>

            <!-- Alertas de Mensagens Flash -->
            <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
                <div class="alert alert--error" style="margin-bottom: 1.5rem;"><%= error_msg %></div>
            <% } %>

            <!-- O action fica vazio para submeter para a URL atual (com o token) -->
            <form method="POST">
                <div class="form-group">
                    <label for="password">Nova Senha</label>
                    <input type="password" id="password" name="password" class="form-control" minlength="6" required autofocus>
                </div>
                <div class="form-group">
                    <label for="confirmPassword">Confirmar Nova Senha</label>
                    <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" required>
                </div>
                <button type="submit" class="btn btn--primary btn--full-width">Redefinir Senha</button>
            </form>
        </div>
    </div>

</body>
</html>
