<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Marketplace Industrial</title>
    
    <!-- Importando os estilos necessários -->
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body class="auth-page">

    <div class="auth-container">
        <h1 class="auth-logo">Marketplace Industrial</h1>

        <div class="auth-box">
            <h2 class="auth-box__title">Recuperar Senha</h2>
            <p class="auth-box__subtitle">Digite seu e-mail para receber as instruções de redefinição.</p>

            <!-- Alertas de Mensagens Flash -->
            <% if (typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
                <div class="alert alert--success" style="margin-bottom: 1.5rem;"><%= success_msg %></div>
            <% } %>
            <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
                <div class="alert alert--error" style="margin-bottom: 1.5rem;"><%= error_msg %></div>
            <% } %>

            <form action="/auth/forgot-password" method="POST">
                <div class="form-group">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name="email" class="form-control" required autofocus>
                </div>
                <button type="submit" class="btn btn--primary btn--full-width">Enviar Link de Recuperação</button>
            </form>

            <div class="auth-box__footer">
                <a href="/auth/login">Voltar para o Login</a>
            </div>
        </div>
    </div>

</body>
</html>
