<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Marketplace Industrial</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="dashboard-container">

        <aside class="sidebar">
            <div class="sidebar__header">
                <a href="/" class="sidebar__logo">
                    <i class="fa-solid fa-industry"></i>
                    <span>Marketplace</span>
                </a>
            </div>
        <nav class="sidebar__menu">
            <a href="/buyer/dashboard" class="<%= currentPath === '/buyer/dashboard' ? 'active' : '' %>">
                <i class="fa-solid fa-table-columns"></i> Meus Pedidos</a>

            <a href="/buyer/quotation" class="<%= currentPath.startsWith('/buyer/quotation') ? 'active' : '' %>">
                <i class="fa-solid fa-box-archive"></i> Minhas Cotações</a>
            
            <a href="/buyer/profile" class="<%= currentPath.startsWith('/buyer/profile') ? 'active' : '' %>">
                <i class="fa-solid fa-receipt"></i> Meu Perfil</a>
        </nav>
            <div class="sidebar__footer">
                <a href="/auth/logout"><i class="fa-solid fa-right-from-bracket"></i> Sair</a>
            </div>
        </aside>

        <!-- Conteúdo Principal da Página -->
        <main class="dashboard-main">
            <%- body %>
        </main>
    </div>
</body>
</html>
